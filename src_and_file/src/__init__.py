from src_and_file.src.talk_for_user import *
from src_and_file.src.script_work_programm import *

# script_dir = os.path.dirname(__file__)

# class_analysisofdata = (pathlib.PurePath(f'{script_dir}')
#         .joinpath('analysisofdata.py'))
"""Создаем путь до нужного исполняемого файла,
где хранится класс для записывания данных
"""

# func_talk_to_user = (pathlib.PurePath(f'{script_dir}')
#         .joinpath('talk_for_user.py'))
# func_talk_to_user = talk_to_user()
"""Создаем путь до нужного исполняемого файла,
где хранится функция для общения с пользователем
"""

